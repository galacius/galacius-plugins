- [File Structure](file_structure.md) — repo layout: pnpm workspace + nested Go module, hexagonal-architecture backend/frontend dir map for both helm (chi router, settings-tab/repository-management surface, config/discovery/index cache) and resources-monitor (gopsutil sampler, footer widget, no gRPC subscribe)
- [Architecture Call Path](architecture_call_path.md) — helm plugin: business calls are HTTP frontend→backend direct; gRPC is a generic pub/sub control channel (2 topics) auth'd via stdin bearer token; install/upgrade is fire-and-forget HTTP + optimistic cache seed, reconciled later over gRPC events. resources-monitor shares the HTTP/auth mechanics but only Emits, never Subscribes.
- [Development Workflow](development_workflow.md) — build/test/lint commands for helm plugin frontend + backend, local install mirroring, design-system dep blocker
- [Footer Widget and Event Listener](footer_widget_and_event_listener.md) — resources-monitor is the first plugin to call `appWideAPI.registerFooterWidget`; app-root vs. cluster-scoped plugin-event listener split, full design in the galacius host repo's memory
